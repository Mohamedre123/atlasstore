-- ============================================================
--  ATLAS Store — أماكن المنتج في الصفحة الرئيسية
-- ------------------------------------------------------------
--  انسخ الملف ده كله والصقه في:
--  Supabase → SQL Editor → New query → Run
--
--  شغّله مرة واحدة بعد catalog.sql. آمن تشغّله أكتر من مرة.
--
--  بيضيف عمود واحد بتتخزّن فيه السكاشن اللي المنتج بيظهر
--  فيها: ["hero","best","category","sale"]
--
--  المنتجات الموجودة دلوقتي بتفضل زي ما هي بالظبط — العمود
--  بيبدأ فاضي، والفاضي معناه «سيبه تلقائي» (يظهر في صف قسمه،
--  وفي العروض لو عليه خصم) — يعني نفس اللي بيحصل حاليًا.
-- ============================================================

alter table public.products
  add column if not exists home_sections jsonb not null default '[]'::jsonb;

comment on column public.products.home_sections is
  'سكاشن الصفحة الرئيسية اللي المنتج بيظهر فيها. فاضي = تلقائي.';


-- ============================================================
--  خلاص. لو ظهرت Success بدون أخطاء يبقى كل حاجة تمام.
-- ============================================================
